{
  "name": "jmhobbs.github.com",
  "version": "1.0.0",
  "description": "This repository is both my personal GitHub page, as well as the generator code that I use to make it.",
  "main": "index.js",
  "scripts": {
    "dev": "npx @11ty/eleventy --input=./src --output=docs --serve",
    "build": "npx @11ty/eleventy --input=./src --output=docs"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "octokit": "^4.0.2",
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-fetch": "^4.0.1"
  }
}
